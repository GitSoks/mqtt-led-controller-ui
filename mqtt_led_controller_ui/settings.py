broker_address = "mqtt_broker"  # the address of the MQTT broker (e.g. "mqtt_broker" for Docker, "localhost" for local)
broker_port = 1883  # the port of the MQTT broker (default: 1883)
broker_username = "ui"  # the username for the MQTT broker
broker_password = "password"  # the password for the MQTT broker

test_devices = True  # if True, the UI will add test devices to the list of devices

led_ring_12_image_path = (
    "./media/led_ring.png"  # the path to the image of the 12-LED ring
)
